# ─── PUERTO ─────────────────────────────────────────────────
# Lo usa docker-compose para mapear el puerto del host y del
# contenedor, y se inyecta como variable de entorno PORT al backend.
# Copia este archivo a .env y ajusta el valor si lo necesitas.
PORT=3010
