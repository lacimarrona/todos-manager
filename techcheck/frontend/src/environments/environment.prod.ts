export const environment = {
  production: true,
  // El backend sirve el frontend desde el mismo origen, así que /api es relativo
  // y funciona en cualquier host/puerto (localhost, NAS, etc.).
  apiUrl: '/api'
};